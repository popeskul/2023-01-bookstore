spring:
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml
    drop-first: true
  shell:
    interactive:
      enabled: false
    main:
      allow-circular-references: true